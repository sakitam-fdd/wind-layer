import { Field } from 'wind-core';
import { TileSource, ImageSource, RenderType } from 'wind-gl-core';
import { WindLayer } from './canvas';
import { WebglLayer } from './webgl';

export { Field, WindLayer, WebglLayer, ImageSource, TileSource, RenderType };

export default WindLayer;
