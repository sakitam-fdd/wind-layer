{
  "name": "ol-wind",
  "version": "1.0.0",
  "description": "ol wind",
  "keywords": [],
  "author": "sakitam-fdd <smilefdd@gmail.com>",
  "license": "MIT",
  "main": "dist/ol-wind.js",
  "module": "dist/ol-wind.esm.js",
  "unpkg": "dist/ol-wind.js",
  "commonjs": "dist/ol-wind.cjs.js",
  "namespace": "OlWind",
  "types": "types/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sakitam-fdd/wind-layer.git",
    "directory": "packages/ol"
  },
  "scripts": {
    "tsc": "tsc --project tsconfig.prod.json",
    "clean": "rimraf dist",
    "dev": "run-p dev:*",
    "dev:rollup": "cross-env NODE_ENV=development modulePath=ol input=src/index.ts rollup -wm -c ../../rollup/dev.config.js",
    "dev:cjs": "cross-env NODE_ENV=development BABEL_ENV=cjs babel src --watch --root-mode upward --out-dir cjs --source-maps --extensions .ts,.tsx --delete-dir-on-start --no-comments",
    "build": "run-p build:*",
    "bulid:rollup": "cross-env NODE_ENV=production modulePath=ol input=src/index.ts rollup -m -c ../../rollup/dev.config.js && cross-env NODE_ENV=production modulePath=ol input=src/index.ts rollup -c ../../rollup/prod.config.js",
    "build:cjs": "cross-env NODE_ENV=production BABEL_ENV=cjs babel src --root-mode upward --out-dir cjs --source-maps --extensions .ts,.tsx --delete-dir-on-start --no-comments",
    "build:esm": "cross-env NODE_ENV=production BABEL_ENV=esm babel src --root-mode upward --out-dir esm --source-maps --extensions .ts,.tsx --delete-dir-on-start --no-comments",
    "lint:ts": "run-p -c lint:ts-*",
    "test": "jest",
    "prepublishOnly": "run-p test",
    "generate:docs": "typedoc --out docs --target es6 --theme minimal --mode file src"
  },
  "bugs": {
    "url": "https://github.com/sakitam-fdd/wind-layer/issues"
  },
  "homepage": "https://github.com/sakitam-fdd/wind-layer#readme",
  "dependencies": {
    "wind-core": "^1.0.0"
  },
  "peerDependencies": {
    "ol": ">=5.0.0"
  },
  "devDependencies": {
    "ol": "^6.0.0"
  }
}
